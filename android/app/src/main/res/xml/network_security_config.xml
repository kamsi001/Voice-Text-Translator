<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration.

    Cleartext (HTTP) traffic is permitted ONLY for local development hosts so
    the debug build can reach a `vercel dev` instance running on the host
    machine:
      - 10.0.2.2 is the Android emulator's alias for the host's loopback.
      - localhost covers physical-device port-forwarding setups.

    All other destinations (including the deployed HTTPS Vercel proxy) continue
    to require TLS under the platform default policy. Keep this list minimal.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
