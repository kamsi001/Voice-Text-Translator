{
  "name": "ai-voice-text-translator-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel serverless proxy for the AI Voice & Text Translator. Holds the AI provider key server-side and exposes POST /api/translate.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.4"
  }
}
